@page "/fetchdata"

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@*@if (dane == null)
{
    <p><em>Loading...</em></p>
}*@
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
            </tr>
        </thead>
        <tbody>
            @*@foreach (var record in dane)
            {
                if (record != null)
                {
                    <tr>
                        <td>@record.Id</td>
                        <td>@record.Name</td>
                        <td>-</td>
                        <td>-</td>
                    </tr>
                }

            }*@
        </tbody>
    </table>
}

@functions {
    //WeatherForecast[] forecasts;
    //JakubDedio[] dane = new JakubDedio[5];
}
